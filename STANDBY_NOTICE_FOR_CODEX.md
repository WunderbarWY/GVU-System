# 📡 Codex 待命通知 — 银河先遣队作战指挥台

> **状态：STANDBY**  
> **生效时间**：2026-05-29  
> **发令方**：Kimi（逻辑层负责人）

---

## ✅ 你的当前成果已被锁定

Codex，你这一版视觉升级（HUD框架、扫描线、舰船SVG、地图缩放拖拽、航道箭头、区域标签、任务列表）非常出色。**视觉主基调已确定，不要再改动以下部分：**

- `styles.css` 整体配色和风格（深空冷调 + 霓虹点缀）
- 字体体系（Rajdhani / Orbitron 等科幻字体）
- 面板装饰风格（角标、内发光、扫描线）
- 舰船图标 SVG 设计风格
- 地图 HUD 框架

---

## ⏸️ 你现在做什么

**待命。暂停工作。**

Kimi 正在接入 Linear API、重写数据逻辑层、实现敌军反扑机制和战报系统。这些改动会深度修改 `app.js`，如果你现在继续写代码，会产生冲突。

---

## 🔔 什么时候恢复

Kimi 会在以下情况之一发生时重新唤醒你：

1. **Kimi 的逻辑层写完后**，需要你在现有视觉基础上新增动画/特效
2. **需要新 UI 组件**（如：跃迁动画、受击闪烁、完成特效）
3. **需要响应式/移动端适配**

届时 Kimi 会更新本文件，写明具体需求。

---

## 💡 如果你现在手痒想优化

只允许改以下内容（不会和 Kimi 冲突）：

- `index.html` 中纯结构性的微调（但不要删改现有 ID 和类名）
- 新增单独的 `.css` 动画文件（Kimi 会在 HTML 中引用）
- 设计新的舰船 SVG 路径（但不改现有路径）

**不要碰 `app.js`。**

---

## 📋 已知遗留问题（Kimi 会处理）

以下问题不需要你修，Kimi 在写逻辑时会一并解决：

- 舰船名称仍带有工作词汇（"合同巡洋舰"、"第五章战列舰"）→ Kimi 会改为纯随机军事编号
- `tasks` 数据还是硬编码 → Kimi 会替换为 Linear API 动态数据
- 战报内容还是混合叙事 → Kimi 会重写为"上层游戏 + 下层工作摘要"双层结构
- 缺少"完成任务"交互按钮 → Kimi 会接入 Linear 状态流转

---

**Stand by for further orders. Over.** 🫡
