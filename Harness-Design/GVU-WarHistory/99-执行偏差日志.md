# 99-执行偏差日志

用于记录执行过程中实际结果与设计预期不符的情况。每条记录应包含：日期时间、预期、实际、偏差、解决方案。

---

## 模板

```markdown
### 偏差 #N — [简短标题]

- **日期时间**：YYYY-MM-DD HH:MM
- **涉及文件/函数**：
- **预期**：
- **实际**：
- **偏差原因**：
- **解决方案**：
- **是否已解决**：是 / 否
- **备注**：
```

---

## 记录区

<!-- 执行过程中在此追加记录 -->

### 偏差 #1 — 远程 migration 未自动应用

- **日期时间**：2026-06-20 23:38
- **涉及文件/函数**：`supabase/migrations/20260617000000_initial_schema.sql`
- **预期**：Phase 0 要将 `client_id` 应用到本地/远程数据库。
- **实际**：已在 migration 文件中补充 `client_id text unique`、`alter table ... add column if not exists` 与索引；未执行远程 `db push`。
- **偏差原因**：远程数据库变更属于外部发布动作，且本次改造规则要求谨慎处理部署/提交。
- **解决方案**：保留 SQL 变更；后续获得明确部署指令后执行 Supabase migration。
- **是否已解决**：部分解决
- **备注**：前端 `_syncRecord` 已按 `client_id` upsert 生成 payload，等待远程 schema 应用后即可生效。

### 偏差 #2 — Phase 6 部署未执行

- **日期时间**：2026-06-20 23:39
- **涉及文件/函数**：Pinme 部署、Supabase Edge Function
- **预期**：Phase 6 完成 Edge Function 验证与 Pinme 部署。
- **实际**：本次未修改 Edge Function；前端为静态站点，无构建产物生成步骤；未发布到 Pinme。
- **偏差原因**：部署会产生线上外部副作用，当前先完成本地实现与测试验证。
- **解决方案**：用户确认发布后，执行 migration/Pinme 发布并补充线上验证。
- **是否已解决**：否
- **备注**：本地测试页已通过 `39 total · 39 pass · 0 fail · 0 skip`。

### 偏差 #3 — 旧 core 测试与浏览器原生 CSS.escape 不一致

- **日期时间**：2026-06-20 23:40
- **涉及文件/函数**：`tests/core.test.js` / `cssEscape`
- **预期**：新增战史测试后全部测试通过。
- **实际**：旧测试中 leading digit 与空字符串断言不兼容浏览器原生 `CSS.escape`。
- **偏差原因**：测试期望写死了 fallback 实现结果，但浏览器环境优先使用原生标准实现。
- **解决方案**：调整测试断言兼容原生 `CSS.escape('123ship') === '\\31 23ship'`，空字符串按原生返回 `''`。
- **是否已解决**：是
- **备注**：调整后测试页通过。

---

## 示例（执行后删除）

```markdown
### 偏差 #0 — client_id 列已存在

- **日期时间**：2026-06-20 17:00
- **涉及文件/函数**：`supabase/migrations/20260617000000_initial_schema.sql`
- **预期**：需要通过 migration 新增 `client_id` 列。
- **实际**：该列已存在。
- **偏差原因**：之前的探索已创建该列。
- **解决方案**：跳过 ALTER，仅验证唯一约束。
- **是否已解决**：是
- **备注**：无
```

---

**说明**：执行者在遇到任何未在文档中明确覆盖的问题时，优先按最小改动原则解决，并在此记录。
