# 10-AI自检清单

执行者在每完成一个 Phase 后，按以下清单逐项自检并勾选。全部勾选后才可进入下一阶段。

---

## Phase -1: 环境验证

- [ ] 项目根目录 `/Users/waiyuen/Documents/Codex/2026-05-29/kimi-kimi-linear-api-linear-linear/` 存在且可写。
- [ ] `app.js` 存在且可读。
- [ ] `src/api/supabase.js` 存在且可读。
- [ ] `supabase/migrations/20260617000000_initial_schema.sql` 存在且可读。
- [ ] Supabase CLI 可用或已记录替代方案。
- [ ] Pinme 部署工具可用或已记录替代方案。

---

## Phase 0: Schema 更新

- [ ] 已检查 `war_history` 表结构。
- [ ] `client_id` 列已存在或已通过 migration/ALTER 新增。
- [ ] `client_id` 具有唯一约束（UNIQUE）。
- [ ] 已通过 SQL 查询验证列存在。

```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'war_history';
```

---

## Phase 1: recordPomodoro + recordSync 修复

- [ ] `recordPomodoro({ added: 1, removed: 0 })` 不抛出 `added is not defined` 错误。
- [ ] `recordPomodoro()` 空参数调用不崩溃。
- [ ] `recordSync` 保存本地记录后调用了 `_syncRecord`。
- [ ] 网络正常时，`recordSync` 触发 Supabase 请求。
- [ ] 网络异常时，`recordSync` 进入离线队列。

---

## Phase 2: Linear done → recordKill

- [ ] `StarshipSync.applyIncremental` 能检测到 issue 从非 done 变为 done。
- [ ] 检测到 done 时调用 `WarHistoryStore.recordKill`。
- [ ] `recordKill` 接收 `{ missionId, shipName, source: 'linear', title }` 参数。
- [ ] 生成的记录 `type` 为 `'kill'`。
- [ ] 同一 `missionId` 不会重复生成 kill 记录。
- [ ] `recordSync('sync-out', ...)` 仍然保留（如有原逻辑）。

---

## Phase 3: 幂等性 + 离线队列

- [ ] `_syncRecord` 上传的 payload 包含 `client_id`。
- [ ] Supabase 使用 upsert 或唯一约束避免重复行。
- [ ] 连续两次上传同一 `client_id` 只产生一条云端记录。
- [ ] 离线或请求失败时记录进入 `localStorage` 队列。
- [ ] 队列 key 为 `gvu_war_history_queue`。
- [ ] 网络恢复时自动调用 `flushQueue`。
- [ ] 同步成功后记录从队列移除。
- [ ] 刷新页面后队列不丢失。

---

## Phase 4: UI 修复

- [ ] `renderCampaign` 使用 `r.time` 显示时间。
- [ ] `renderCampaign` 使用 `r.shipName` 显示飞船名。
- [ ] `renderCampaign` 使用 `r.desc` 显示描述。
- [ ] `renderCampaign` 不出现 `undefined`。
- [ ] `renderWarHistory` 字段映射正确。
- [ ] `G.warHistory` 中不再写入 `type: 'victory'`。
- [ ] 旧代码读取 `G.warHistory` 仍能获得最新记录（通过 getter 或引用同步）。

---

## Phase 5: 测试

- [ ] 新增战争历史测试文件。
- [ ] `recordKill` 测试通过。
- [ ] `recordPomodoro` 测试通过。
- [ ] `recordSync` 测试通过。
- [ ] 幂等性测试通过（同一 missionId 只一条记录）。
- [ ] 离线队列测试通过。
- [ ] UI 渲染测试通过（字段正确）。
- [ ] 既有测试未被破坏。

---

## Phase 6: 部署

- [ ] Edge Function（如需要）已本地 serve 验证。
- [ ] Edge Function（如需要）已部署到 Supabase。
- [ ] 前端构建成功。
- [ ] Pinme 部署成功。
- [ ] 线上页面可访问。

---

## Phase 7: 最终验证

- [ ] 手动完成 mission 后战争历史出现 kill 记录。
- [ ] Linear 完成 issue 后战争历史出现 kill 记录。
- [ ] 重复触发不生成重复记录。
- [ ] 离线后恢复网络，队列自动同步。
- [ ] 战役时间线显示正确。
- [ ] 情报面板显示正确。
- [ ] 所有测试通过。
- [ ] 线上功能与本地一致。

---

## 最终放行标准

只有当以上所有 Phase 的检查项全部勾选后，整个改造才可视为完成。若有未勾选项，返回对应 Phase 修复并更新 `TODO.md` 与 `99-执行偏差日志.md`。

---

**下一步**：开始执行，按 `TODO.md` 推进。
